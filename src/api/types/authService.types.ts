export interface SigninProps {
    email: string;
    password: string;
}

export interface SignupProps {
    email: string;
    password: string;
    firstName: string;
    lastName: string;
}

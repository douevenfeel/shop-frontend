import styled from 'styled-components';
import { THEME } from 'utils/constants';

export const ManagerOrderCardStyled = styled.div`
    width: 400px;
    min-width: 288px;
    background: ${THEME.darkerGray};
    border-radius: 8px;
    padding: 20px;
    gap: 20px;
`;
